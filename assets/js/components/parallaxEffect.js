/**
 * Parallax Effect Component
 * DISABLED - was causing stuttering issues
 */

export function initParallaxEffect() {
  console.log('⚡ Parallax effects disabled for smooth scrolling');
  // All parallax effects disabled
}
